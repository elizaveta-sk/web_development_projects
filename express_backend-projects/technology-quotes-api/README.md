# Technology Quotes API

A small Express API and browser interface for exploring quotes about programming, computers, and technology.

## What it does

- Shows a random quote
- Lists all quotes or filters them by author
- Adds a quote through the API

## Run locally

```bash
npm install
npm start
```

Open `http://localhost:4001` in a browser. The API is available at `/api/quotes`.
